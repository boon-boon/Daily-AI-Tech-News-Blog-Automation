# Daily Tech Pulse — Python dependencies
#
# This project no longer runs its own LLM pipeline. The daily Claude
# scheduled task does the writing; the only Python step left is the static
# site builder, which just renders JSON → HTML.

markdown>=3.6
